{
  "name": "@repo/utils",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --silent"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:",
    "@repo/config-build-tools": "workspace:",
    "eslint": "catalog:",
    "vitest": "catalog:"
  }
}
